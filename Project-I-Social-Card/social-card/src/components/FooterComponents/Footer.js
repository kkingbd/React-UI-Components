import React from 'react';
import './Footer.css';

function Footer(){
    return (
        <div className = 'footer-title'>
            <h2 className = 'display-name' >Get started with React </h2>
            <p className = 'text' > React makes it painless to creat interactive UIs. Design simple views for each state in your application</p>
            <p className = 'link-text'> react.org</p>
        </div>
    );
};

export default Footer;